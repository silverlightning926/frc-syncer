{
	"name": "python-models",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"postinstall": "pdm install",
		"lint": "pdm run isort . && pdm run black . && pdm run flake8 ./src/",
		"lint:check": "pwd"
	}
}
