[project]
name = "processor"
version = "0.1.0"
description = "The Python module that handles all the data crunching and processing."
authors = [
    {name = "Siddharth Rao", email = "siddrao.926@gmail.com"},
]
dependencies = ["python-models @ file:///${PROJECT_ROOT}/../python-models"]
requires-python = ">=3.13"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
